services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
    env_file:
      - .env
    restart: always

# Note: In a full MLOps setup, we would also add a 'frontend' service here.
